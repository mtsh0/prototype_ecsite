<h1>Users#index</h1>
<p>Find me in app/views/users/index.html.erb</p>

<% provide(:title, 'All users') %>
<h1>All users</h1>

<ul class="users">
  <% @users.each do |user| %>
      <li><%= user.email %></li>
      <li><%= link_to "ユーザー#{user.id}", user_path(user) %></li>
  <% end %>
</ul>